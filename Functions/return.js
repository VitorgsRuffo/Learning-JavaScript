//return keyword:
    function square(num){
        return num * num;
    }

    var squareOfFour = square(4);
    console.log(squareOfFour);  //16

    //Obs: If you don't return something the function will return undefined by default.
    //Obs: a 'return' will end the execution of its function.