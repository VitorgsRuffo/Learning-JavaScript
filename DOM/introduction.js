//Document Object Model: interface between JS and HTML+CSS.
//it allows JS to manipulate HTML and CSS.

//all HTML tags are turned into JS objects. They are all together inside a main Object called 'Document'.
//to check the structure of the document object of a page: console.dir(document); for example the entire body of the page was turned into an object that is inside the main one called document

